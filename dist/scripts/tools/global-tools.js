/**
 * |-----------------------------------------------------|
 * |                                                     |
 * |                 Global Handler Tool                 |
 * |                                                     |
 * |-----------------------------------------------------|
 */
/* -- Imports -- */
import App from "../variables/index.js";
/**
 * Create a new set of key pair that is globally accessible at your project runtime.
 */
function MakeGlobal(accessKey, data) {
    try {
    }
    catch (e) {
        console.error("", e);
    }
}
App.MakeGlobal = MakeGlobal;
